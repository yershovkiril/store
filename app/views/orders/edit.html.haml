= provide(:title, t('cart.shopping_cart'))
.container
  #basket
    .box
      %h1 Shopping cart
      - if @order.empty?
        .alert.alert-info Your cart is empty
        = link_to t('cart.continue'), books_path, class: "btn btn-default"
      - else
        = render('form')